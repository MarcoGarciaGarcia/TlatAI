
<section id="hero" class="relative h-screen w-full overflow-hidden bg-gradient-to-br from-gray-900 to-blue-900 text-white">
  <!-- Fondo con animación de partículas (opcional) -->
  <div class="absolute inset-0 z-0 opacity-0">
    <div class="absolute h-full w-full animate-pulse bg-[url('/grid-pattern.svg')]"></div>
  </div>

  <!-- Contenido principal -->
  <div class="relative z-10 mx-auto flex h-full max-w-6xl flex-col justify-center px-8 text-center">
    <header class="space-y-6">
      <h1 class="text-5xl font-bold leading-tight sm:text-6xl md:text-7xl lg:text-8xl">
        <span class="bg-gradient-to-r from-cyan-400 to-blue-600 bg-clip-text text-transparent">
          Transformers
        </span>
      </h1>
      <h2 class="text-2xl font-medium text-gray-300 sm:text-3xl md:text-4xl">
        Revolucionando el Procesamiento del Lenguaje Natural
      </h2>
      <p class="mx-auto max-w-2xl text-lg text-gray-400 md:text-xl">
        Descubre la arquitectura que está cambiando la inteligencia artificial con mecanismos de atención auto-supervisada
      </p>
    </header>

    <!-- CTA -->
    <div class="mt-12 flex justify-center gap-4">
      <a href="#arquitectura" class="rounded-lg bg-blue-600 px-8 py-3 font-medium hover:bg-blue-700">
        Ver Arquitectura
      </a>
      <a href="#aplicaciones" class="rounded-lg border border-blue-400 px-8 py-3 font-medium hover:bg-blue-900/30">
        Aplicaciones
      </a>
    </div>
  </div>

  <!-- Scroll indicator (opcional) -->
  <div class="absolute bottom-8 left-1/2 z-10 -translate-x-1/2 animate-bounce">
    <svg class="h-8 w-8 text-blue-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"/>
    </svg>
  </div>
</section>